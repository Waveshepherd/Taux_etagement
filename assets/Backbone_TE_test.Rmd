---
title: "![](OFB_Logo.jpg){width=25%} Tableau récapitulatif des différents cours d'eau classé en liste 2 et mis en qualités dans Geobs"
date:  "`r format(Sys.time(), '%d %B %Y')`"
output: 
  html_document:
    highlight: pygment
    css: "style.css"
runtime : shiny 
params: 
  CE: 
    value: "l'yères"
editor_options: 
  chunk_output_type: console
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(
  echo = FALSE,
  message = FALSE,
  warning = FALSE
  )

library(tidyverse)
library(sf)

load("../data_prepared/ROE_data.RData")

```
<br>

#[Résumé à l'échelle de la région]{.underline}

```{r Tab résumé CE L2}

'%>%' <- dplyr::'%>%'

#Calcul du TE pour le tableau résumé

#Hauteurs artificielles cumulées
tab_h <- ROE_Normandie_H %>%  sf::st_drop_geometry() %>% 
  dplyr::filter(hauteur != is.na(hauteur),
                nom_CE != "NA") %>% 
  dplyr::group_by(nom_CE) %>% 
  dplyr::add_count(nom_CE, name = "nb_ouv") %>% 
  dplyr::mutate(H_cum = sum(hauteur)) %>% 
  dplyr::filter(ouv_liaison == "ouvrage principal") %>% 
  dplyr::add_count(nom_CE, name = "nb_ouvp") %>% 
  dplyr::ungroup() %>% 
  dplyr::select("nom_CE", "H_cum", "nb_ouv", "nb_ouvp") %>% 
  unique()
  
Info <- dplyr::left_join(Info, tab_h, dplyr::join_by('nom_CE'))

#Obtention du TE

Info <- Info %>% dplyr::mutate(TE = round(H_cum/deni_nat*100,2)) %>% 
  dplyr::relocate("nb_ouvp","nb_ouv", .before =  "H_cum") 

Info <- Info %>% dplyr::filter(nom_CE %in% ROE_Normandie_H$nom_CE) # à retirer

DT::datatable(Info, colnames = c(
    "Code de la Masse d'eau",
    "Nom du cours d'eau",
    "Longueur du cours d'eau (en km)",
    "Altitute du point le plus en aval (en m)",
    "Altitude du point le plus en amont (en m)",
    "Dénivelé naturel (en m)",
    "Pente naturelle (en ‰)",
    "Nombre d'ouvrage sur le cours principal",
    "Nombre d'ouvrage sur le cours d'eau",
    "Hauteurs artificielles cumulées",
    "Taux d'étagement (en %)"
    
    
  ))

```